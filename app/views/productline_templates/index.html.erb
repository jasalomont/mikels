<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All productlines
    </h1>

    <a href="/productlines/new" class="btn btn-block btn-outline-secondary">
      Add a new productline
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "productline_templates/productlines_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Name
        </th>

        <th>
          Description
        </th>

        <th>
          Subcategory
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @productlines.each do |productline| %>
      <tr>
        <!-- Display productline.id start -->
        <td>
          <%= productline.id %>
        </td>
        <!-- Display productline.id end -->

        <!-- Display productline.name start -->
        <td>
          <%= productline.name %>
        </td>
        <!-- Display productline.name end -->

        <!-- Display productline.description start -->
        <td>
          <%= productline.description %>
        </td>
        <!-- Display productline.description end -->

        <!-- Display productline.subcategory_id start -->
        <td>
          <%= productline.subcategory_id %>
        </td>
        <!-- Display productline.subcategory_id end -->

        <!-- Display productline.created_at start -->
        <td>
          <%= time_ago_in_words(productline.created_at) %> ago
        </td>
        <!-- Display productline.created_at end -->

        <!-- Display productline.updated_at start -->
        <td>
          <%= time_ago_in_words(productline.updated_at) %> ago
        </td>
        <!-- Display productline.updated_at end -->

        <td>
          <a href="/productlines/<%= productline.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @productlines, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
